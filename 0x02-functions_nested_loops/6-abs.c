#include "main.h"

/**
 * this is to return the absoulute value
 * of any number
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else if (n == 0)
	{
		return (n);
	}
	else
	{
		return (n);
	}
}
